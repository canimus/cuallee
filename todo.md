`is_complete` +family #generic {cm:2025-02-09}
`is_empty` +family #generic {cm:2025-02-09}
`are_complete` +family #generic {cm:2025-02-09}
`is_unique` +family #generic {cm:2025-02-09}
`is_primary_key` +family #generic {cm:2025-02-09}
`are_unique` +family #generic {cm:2025-02-09}
`is_composite_key` +family #generic {cm:2025-02-09}
`is_greater_than` +family #numeric {cm:2025-02-09}
`is_positive` +family #numeric {cm:2025-02-09}
`is_greater_or_equal_than` +family #numeric {cm:2025-02-09}
`is_in_millions` +family #numeric {cm:2025-02-09}
`is_in_billions` +family #numeric {cm:2025-02-09}
`is_less_than` +family #numeric {cm:2025-02-09}
`is_negative` +family #numeric {cm:2025-02-09}
`is_less_or_equal_than` +family #numeric {cm:2025-02-09}
`is_equal_than` +family #numeric {cm:2025-02-09}
`has_pattern` +family #string {cm:2025-02-09}
`is_legit` +family #string {cm:2025-02-09}
`has_min` +family #stats {cm:2025-02-09}
`has_max` +family #stats {cm:2025-02-09}
`has_std` +family #stats {cm:2025-02-09}
`has_mean` +family #stats {cm:2025-02-09}
`has_sum` +family #stats {cm:2025-02-09}
`is_between` +family #generic {cm:2025-02-09}
`not_contained_in` +family #generic {cm:2025-02-09}
`not_in` +family #generic {cm:2025-02-09}
`is_contained_in` +family #generic {cm:2025-02-09}
`is_in` +family #generic {cm:2025-02-09}
`is_t_minus_n` +family #datetime
`is_t_minus_1` +family #datetime
`is_t_minus_2` +family #datetime
`is_t_minus_3` +family #datetime
`is_yesterday` +family #datetime
`is_today` +family #datetime
`has_percentile` +family #stats {cm:2025-02-09}
`is_inside_interquartile_range` +family #stats {cm:2025-02-09}
`has_correlation` +family #ml {cm#ml-02-09} {cm:2025-02-09}
`satisfies` +family #generic {cm:2025-02-09}
`has_cardinality` +family #ml {cm:2025-02-09}
`has_infogain` +family #ml {cm:2025-02-09}
`has_entropy` +family #ml {cm:2025-02-09}
`is_on_weekday` +family #datetime
`is_on_weekend` +family #datetime
`is_on_monday` +family #datetime
`is_on_tuesday` +family #datetime
`is_on_wednesday` +family #datetime
`is_on_thursday` +family #datetime
`is_on_friday` +family #datetime
`is_on_saturday` +family #datetime
`is_on_sunday` +family #datetime
`is_on_schedule` +family #datetime
`is_daily` +family #datetime
`has_workflow` +family #business
`is_custom` +family #generic
`is_complete` +engine @pyspark #generic
`is_empty` +engine @pyspark #generic
`are_complete` +engine @pyspark #generic
`is_unique` +engine @pyspark #generic
`is_primary_key` +engine @pyspark #generic
`are_unique` +engine @pyspark #generic
`is_composite_key` +engine @pyspark #generic
`is_greater_than` +engine @pyspark #numeric
`is_positive` +engine @pyspark #numeric
`is_greater_or_equal_than` +engine @pyspark #numeric
`is_in_millions` +engine @pyspark #numeric
`is_in_billions` +engine @pyspark #numeric
`is_less_than` +engine @pyspark #numeric
`is_negative` +engine @pyspark #numeric
`is_less_or_equal_than` +engine @pyspark #numeric
`is_equal_than` +engine @pyspark #numeric
`has_pattern` +engine @pyspark #string
`is_legit` +engine @pyspark #string
`has_min` +engine @pyspark #stats
`has_max` +engine @pyspark #stats
`has_std` +engine @pyspark #stats
`has_mean` +engine @pyspark #stats
`has_sum` +engine @pyspark #stats
`is_between` +engine @pyspark #generic
`not_contained_in` +engine @pyspark #generic
`not_in` +engine @pyspark #generic
`is_contained_in` +engine @pyspark #generic
`is_in` +engine @pyspark #generic
`is_t_minus_n` +engine @pyspark #datetime
`is_t_minus_1` +engine @pyspark #datetime
`is_t_minus_2` +engine @pyspark #datetime
`is_t_minus_3` +engine @pyspark #datetime
`is_yesterday` +engine @pyspark #datetime
`is_today` +engine @pyspark #datetime
`has_percentile` +engine @pyspark #stats
`is_inside_interquartile_range` +engine @pyspark #stats
`has_correlation` +engine @pyspark #ml
`satisfies` +engine @pyspark #generic
`has_cardinality` +engine @pyspark #ml
`has_infogain` +engine @pyspark #ml
`has_entropy` +engine @pyspark #ml
`is_on_weekday` +engine @pyspark #datetime
`is_on_weekend` +engine @pyspark #datetime
`is_on_monday` +engine @pyspark #datetime
`is_on_tuesday` +engine @pyspark #datetime
`is_on_wednesday` +engine @pyspark #datetime
`is_on_thursday` +engine @pyspark #datetime
`is_on_friday` +engine @pyspark #datetime
`is_on_saturday` +engine @pyspark #datetime
`is_on_sunday` +engine @pyspark #datetime
`is_on_schedule` +engine @pyspark #datetime
`is_daily` +engine @pyspark #datetime
`has_workflow` +engine @pyspark #business
`is_custom` +engine @pyspark #generic
`is_complete` +engine @pandas #generic
`is_empty` +engine @pandas #generic
`are_complete` +engine @pandas #generic
`is_unique` +engine @pandas #generic
`is_primary_key` +engine @pandas #generic
`are_unique` +engine @pandas #generic
`is_composite_key` +engine @pandas #generic
`is_greater_than` +engine @pandas #numeric
`is_positive` +engine @pandas #numeric
`is_greater_or_equal_than` +engine @pandas #numeric
`is_in_millions` +engine @pandas #numeric
`is_in_billions` +engine @pandas #numeric
`is_less_than` +engine @pandas #numeric
`is_negative` +engine @pandas #numeric
`is_less_or_equal_than` +engine @pandas #numeric
`is_equal_than` +engine @pandas #numeric
`has_pattern` +engine @pandas #string
`is_legit` +engine @pandas #string
`has_min` +engine @pandas #stats
`has_max` +engine @pandas #stats
`has_std` +engine @pandas #stats
`has_mean` +engine @pandas #stats
`has_sum` +engine @pandas #stats
`is_between` +engine @pandas #generic
`not_contained_in` +engine @pandas #generic
`not_in` +engine @pandas #generic
`is_contained_in` +engine @pandas #generic
`is_in` +engine @pandas #generic
`is_t_minus_n` +engine @pandas #datetime
`is_t_minus_1` +engine @pandas #datetime
`is_t_minus_2` +engine @pandas #datetime
`is_t_minus_3` +engine @pandas #datetime
`is_yesterday` +engine @pandas #datetime
`is_today` +engine @pandas #datetime
`has_percentile` +engine @pandas #stats
`is_inside_interquartile_range` +engine @pandas #stats
`has_correlation` +engine @pandas #ml
`satisfies` +engine @pandas #generic
`has_cardinality` +engine @pandas #ml
\1#ml$
`has_entropy` +engine @pandas #ml
`is_on_weekday` +engine @pandas #datetime
`is_on_weekend` +engine @pandas #datetime
`is_on_monday` +engine @pandas #datetime
`is_on_tuesday` +engine @pandas #datetime
`is_on_wednesday` +engine @pandas #datetime
`is_on_thursday` +engine @pandas #datetime
`is_on_friday` +engine @pandas #datetime
`is_on_saturday` +engine @pandas #datetime
`is_on_sunday` +engine @pandas #datetime
`is_on_schedule` +engine @pandas #datetime
`is_daily` +engine @pandas #datetime
`has_workflow` +engine @pandas #business
`is_custom` +engine @pandas #generic
`is_complete` +engine @polars #generic
`is_empty` +engine @polars #generic
`are_complete` +engine @polars #generic
`is_unique` +engine @polars #generic
`is_primary_key` +engine @polars #generic
`are_unique` +engine @polars #generic
`is_composite_key` +engine @polars #generic
`is_greater_than` +engine @polars #numeric
`is_positive` +engine @polars #numeric
`is_greater_or_equal_than` +engine @polars #numeric
`is_in_millions` +engine @polars #numeric
`is_in_billions` +engine @polars #numeric
`is_less_than` +engine @polars #numeric
`is_negative` +engine @polars #numeric
`is_less_or_equal_than` +engine @polars #numeric
`is_equal_than` +engine @polars #numeric
`has_pattern` +engine @polars #string
`is_legit` +engine @polars #string
`has_min` +engine @polars #stats
`has_max` +engine @polars #stats
`has_std` +engine @polars #stats
`has_mean` +engine @polars #stats
`has_sum` +engine @polars #stats
`is_between` +engine @polars #generic
`not_contained_in` +engine @polars #generic
`not_in` +engine @polars #generic
`is_contained_in` +engine @polars #generic
`is_in` +engine @polars #generic
`is_t_minus_n` +engine @polars #datetime
`is_t_minus_1` +engine @polars #datetime
`is_t_minus_2` +engine @polars #datetime
`is_t_minus_3` +engine @polars #datetime
`is_yesterday` +engine @polars #datetime
`is_today` +engine @polars #datetime
`has_percentile` +engine @polars #stats
`is_inside_interquartile_range` +engine @polars #stats
`has_correlation` +engine @polars #ml
`satisfies` +engine @polars #generic
`has_cardinality` +engine @polars #ml
`has_infogain` +engine @polars #ml
`has_entropy` +engine @polars #ml
`is_on_weekday` +engine @polars #datetime
`is_on_weekend` +engine @polars #datetime
`is_on_monday` +engine @polars #datetime
`is_on_tuesday` +engine @polars #datetime
`is_on_wednesday` +engine @polars #datetime
`is_on_thursday` +engine @polars #datetime
`is_on_friday` +engine @polars #datetime
`is_on_saturday` +engine @polars #datetime
`is_on_sunday` +engine @polars #datetime
`is_on_schedule` +engine @polars #datetime
`is_daily` +engine @polars #datetime
`has_workflow` +engine @polars #business
`is_custom` +engine @polars #generic
`is_complete` +engine @duckdb #generic
`is_empty` +engine @duckdb #generic
`are_complete` +engine @duckdb #generic
`is_unique` +engine @duckdb #generic
`is_primary_key` +engine @duckdb #generic
`are_unique` +engine @duckdb #generic
`is_composite_key` +engine @duckdb #generic
`is_greater_than` +engine @duckdb #numeric
`is_positive` +engine @duckdb #numeric
`is_greater_or_equal_than` +engine @duckdb #numeric
`is_in_millions` +engine @duckdb #numeric
`is_in_billions` +engine @duckdb #numeric
`is_less_than` +engine @duckdb #numeric
`is_negative` +engine @duckdb #numeric
`is_less_or_equal_than` +engine @duckdb #numeric
`is_equal_than` +engine @duckdb #numeric
`has_pattern` +engine @duckdb #string
`is_legit` +engine @duckdb #string
`has_min` +engine @duckdb #stats
`has_max` +engine @duckdb #stats
`has_std` +engine @duckdb #stats
`has_mean` +engine @duckdb #stats
`has_sum` +engine @duckdb #stats
`is_between` +engine @duckdb #generic
`not_contained_in` +engine @duckdb #generic
`not_in` +engine @duckdb #generic
`is_contained_in` +engine @duckdb #generic
`is_in` +engine @duckdb #generic
`is_t_minus_n` +engine @duckdb #datetime
`is_t_minus_1` +engine @duckdb #datetime
`is_t_minus_2` +engine @duckdb #datetime
`is_t_minus_3` +engine @duckdb #datetime
`is_yesterday` +engine @duckdb #datetime
`is_today` +engine @duckdb #datetime
`has_percentile` +engine @duckdb #stats
`is_inside_interquartile_range` +engine @duckdb #stats
`has_correlation` +engine @duckdb #ml
`satisfies` +engine @duckdb #generic
`has_cardinality` +engine @duckdb #ml
`has_infogain` +engine @duckdb #ml
`has_entropy` +engine @duckdb #ml
`is_on_weekday` +engine @duckdb #datetime
`is_on_weekend` +engine @duckdb #datetime
`is_on_monday` +engine @duckdb #datetime
`is_on_tuesday` +engine @duckdb #datetime
`is_on_wednesday` +engine @duckdb #datetime
`is_on_thursday` +engine @duckdb #datetime
`is_on_friday` +engine @duckdb #datetime
`is_on_saturday` +engine @duckdb #datetime
`is_on_sunday` +engine @duckdb #datetime
`is_on_schedule` +engine @duckdb #datetime
`is_daily` +engine @duckdb #datetime
`has_workflow` +engine @duckdb #business
`is_custom` +engine @duckdb #generic
`is_complete` +engine @bigquery #generic
`is_empty` +engine @bigquery #generic
`are_complete` +engine @bigquery #generic
`is_unique` +engine @bigquery #generic
`is_primary_key` +engine @bigquery #generic
`are_unique` +engine @bigquery #generic
`is_composite_key` +engine @bigquery #generic
`is_greater_than` +engine @bigquery #numeric
`is_positive` +engine @bigquery #numeric
`is_greater_or_equal_than` +engine @bigquery #numeric
`is_in_millions` +engine @bigquery #numeric
`is_in_billions` +engine @bigquery #numeric
`is_less_than` +engine @bigquery #numeric
`is_negative` +engine @bigquery #numeric
`is_less_or_equal_than` +engine @bigquery #numeric
`is_equal_than` +engine @bigquery #numeric
`has_pattern` +engine @bigquery #string
`is_legit` +engine @bigquery #string
`has_min` +engine @bigquery #stats
`has_max` +engine @bigquery #stats
`has_std` +engine @bigquery #stats
`has_mean` +engine @bigquery #stats
`has_sum` +engine @bigquery #stats
`is_between` +engine @bigquery #generic
`not_contained_in` +engine @bigquery #generic
`not_in` +engine @bigquery #generic
`is_contained_in` +engine @bigquery #generic
`is_in` +engine @bigquery #generic
`is_t_minus_n` +engine @bigquery #datetime
`is_t_minus_1` +engine @bigquery #datetime
`is_t_minus_2` +engine @bigquery #datetime
`is_t_minus_3` +engine @bigquery #datetime
`is_yesterday` +engine @bigquery #datetime
`is_today` +engine @bigquery #datetime
`has_percentile` +engine @bigquery #stats
`is_inside_interquartile_range` +engine @bigquery #stats
`has_correlation` +engine @bigquery #ml
`satisfies` +engine @bigquery #generic
`has_cardinality` +engine @bigquery #ml
`has_infogain` +engine @bigquery #ml
`has_entropy` +engine @bigquery #ml
`is_on_weekday` +engine @bigquery #datetime
`is_on_weekend` +engine @bigquery #datetime
`is_on_monday` +engine @bigquery #datetime
`is_on_tuesday` +engine @bigquery #datetime
`is_on_wednesday` +engine @bigquery #datetime
`is_on_thursday` +engine @bigquery #datetime
`is_on_friday` +engine @bigquery #datetime
`is_on_saturday` +engine @bigquery #datetime
`is_on_sunday` +engine @bigquery #datetime
`is_on_schedule` +engine @bigquery #datetime
`is_daily` +engine @bigquery #datetime
`has_workflow` +engine @bigquery #business
`is_custom` +engine @bigquery #generic
`is_complete` +engine @snowpark #generic
`is_empty` +engine @snowpark #generic
`are_complete` +engine @snowpark #generic
`is_unique` +engine @snowpark #generic
`is_primary_key` +engine @snowpark #generic
`are_unique` +engine @snowpark #generic
`is_composite_key` +engine @snowpark #generic
`is_greater_than` +engine @snowpark #numeric
`is_positive` +engine @snowpark #numeric
`is_greater_or_equal_than` +engine @snowpark #numeric
`is_in_millions` +engine @snowpark #numeric
`is_in_billions` +engine @snowpark #numeric
`is_less_than` +engine @snowpark #numeric
`is_negative` +engine @snowpark #numeric
`is_less_or_equal_than` +engine @snowpark #numeric
`is_equal_than` +engine @snowpark #numeric
`has_pattern` +engine @snowpark #string
`is_legit` +engine @snowpark #string
`has_min` +engine @snowpark #stats
`has_max` +engine @snowpark #stats
`has_std` +engine @snowpark #stats
`has_mean` +engine @snowpark #stats
`has_sum` +engine @snowpark #stats
`is_between` +engine @snowpark #generic
`not_contained_in` +engine @snowpark #generic
`not_in` +engine @snowpark #generic
`is_contained_in` +engine @snowpark #generic
`is_in` +engine @snowpark #generic
`is_t_minus_n` +engine @snowpark #datetime
`is_t_minus_1` +engine @snowpark #datetime
`is_t_minus_2` +engine @snowpark #datetime
`is_t_minus_3` +engine @snowpark #datetime
`is_yesterday` +engine @snowpark #datetime
`is_today` +engine @snowpark #datetime
`has_percentile` +engine @snowpark #stats
`is_inside_interquartile_range` +engine @snowpark #stats
`has_correlation` +engine @snowpark #ml
`satisfies` +engine @snowpark #generic
`has_cardinality` +engine @snowpark #ml
`has_infogain` +engine @snowpark #ml
`has_entropy` +engine @snowpark #ml
`is_on_weekday` +engine @snowpark #datetime
`is_on_weekend` +engine @snowpark #datetime
`is_on_monday` +engine @snowpark #datetime
`is_on_tuesday` +engine @snowpark #datetime
`is_on_wednesday` +engine @snowpark #datetime
`is_on_thursday` +engine @snowpark #datetime
`is_on_friday` +engine @snowpark #datetime
`is_on_saturday` +engine @snowpark #datetime
`is_on_sunday` +engine @snowpark #datetime
`is_on_schedule` +engine @snowpark #datetime
`is_daily` +engine @snowpark #datetime
`has_workflow` +engine @snowpark #business
`is_custom` +engine @snowpark #generic
`is_complete` +test @pyspark #generic
`is_empty` +test @pyspark #generic
`are_complete` +test @pyspark #generic
`is_unique` +test @pyspark #generic
`is_primary_key` +test @pyspark #generic
`are_unique` +test @pyspark #generic
`is_composite_key` +test @pyspark #generic
`is_greater_than` +test @pyspark #numeric
`is_positive` +test @pyspark #numeric
`is_greater_or_equal_than` +test @pyspark #numeric
`is_in_millions` +test @pyspark #numeric
`is_in_billions` +test @pyspark #numeric
`is_less_than` +test @pyspark #numeric
`is_negative` +test @pyspark #numeric
`is_less_or_equal_than` +test @pyspark #numeric
`is_equal_than` +test @pyspark #numeric
`has_pattern` +test @pyspark #string
`is_legit` +test @pyspark #string
`has_min` +test @pyspark #stats
`has_max` +test @pyspark #stats
`has_std` +test @pyspark #stats
`has_mean` +test @pyspark #stats
`has_sum` +test @pyspark #stats
`is_between` +test @pyspark #generic
`not_contained_in` +test @pyspark #generic
`not_in` +test @pyspark #generic
`is_contained_in` +test @pyspark #generic
`is_in` +test @pyspark #generic
`is_t_minus_n` +test @pyspark #datetime
`is_t_minus_1` +test @pyspark #datetime
`is_t_minus_2` +test @pyspark #datetime
`is_t_minus_3` +test @pyspark #datetime
`is_yesterday` +test @pyspark #datetime
`is_today` +test @pyspark #datetime
`has_percentile` +test @pyspark #stats
`is_inside_interquartile_range` +test @pyspark #stats
`has_correlation` +test @pyspark #ml
`satisfies` +test @pyspark #generic
`has_cardinality` +test @pyspark #ml
`has_infogain` +test @pyspark #ml
`has_entropy` +test @pyspark #ml
`is_on_weekday` +test @pyspark #datetime
`is_on_weekend` +test @pyspark #datetime
`is_on_monday` +test @pyspark #datetime
`is_on_tuesday` +test @pyspark #datetime
`is_on_wednesday` +test @pyspark #datetime
`is_on_thursday` +test @pyspark #datetime
`is_on_friday` +test @pyspark #datetime
`is_on_saturday` +test @pyspark #datetime
`is_on_sunday` +test @pyspark #datetime
`is_on_schedule` +test @pyspark #datetime
`is_daily` +test @pyspark #datetime
`has_workflow` +test @pyspark #business
`is_custom` +test @pyspark #generic
`is_complete` +test @pandas #generic
`is_empty` +test @pandas #generic
`are_complete` +test @pandas #generic
`is_unique` +test @pandas #generic
`is_primary_key` +test @pandas #generic
`are_unique` +test @pandas #generic
`is_composite_key` +test @pandas #generic
`is_greater_than` +test @pandas #numeric
`is_positive` +test @pandas #numeric
`is_greater_or_equal_than` +test @pandas #numeric
`is_in_millions` +test @pandas #numeric
`is_in_billions` +test @pandas #numeric
`is_less_than` +test @pandas #numeric
`is_negative` +test @pandas #numeric
`is_less_or_equal_than` +test @pandas #numeric
`is_equal_than` +test @pandas #numeric
`has_pattern` +test @pandas #string
`is_legit` +test @pandas #string
`has_min` +test @pandas #stats
`has_max` +test @pandas #stats
`has_std` +test @pandas #stats
`has_mean` +test @pandas #stats
`has_sum` +test @pandas #stats
`is_between` +test @pandas #generic
`not_contained_in` +test @pandas #generic
`not_in` +test @pandas #generic
`is_contained_in` +test @pandas #generic
`is_in` +test @pandas #generic
`is_t_minus_n` +test @pandas #datetime
`is_t_minus_1` +test @pandas #datetime
`is_t_minus_2` +test @pandas #datetime
`is_t_minus_3` +test @pandas #datetime
`is_yesterday` +test @pandas #datetime
`is_today` +test @pandas #datetime
`has_percentile` +test @pandas #stats
`is_inside_interquartile_range` +test @pandas #stats
`has_correlation` +test @pandas #ml
`satisfies` +test @pandas #generic
`has_cardinality` +test @pandas #ml
`has_infogain` +test @pandas #ml
`has_entropy` +test @pandas #ml
`is_on_weekday` +test @pandas #datetime
`is_on_weekend` +test @pandas #datetime
`is_on_monday` +test @pandas #datetime
`is_on_tuesday` +test @pandas #datetime
`is_on_wednesday` +test @pandas #datetime
`is_on_thursday` +test @pandas #datetime
`is_on_friday` +test @pandas #datetime
`is_on_saturday` +test @pandas #datetime
`is_on_sunday` +test @pandas #datetime
`is_on_schedule` +test @pandas #datetime
`is_daily` +test @pandas #datetime
`has_workflow` +test @pandas #business
`is_custom` +test @pandas #generic
`is_complete` +test @polars #generic
`is_empty` +test @polars #generic
`are_complete` +test @polars #generic
`is_unique` +test @polars #generic
`is_primary_key` +test @polars #generic
`are_unique` +test @polars #generic
`is_composite_key` +test @polars #generic
`is_greater_than` +test @polars #numeric
`is_positive` +test @polars #numeric
`is_greater_or_equal_than` +test @polars #numeric
`is_in_millions` +test @polars #numeric
`is_in_billions` +test @polars #numeric
`is_less_than` +test @polars #numeric
`is_negative` +test @polars #numeric
`is_less_or_equal_than` +test @polars #numeric
`is_equal_than` +test @polars #numeric
`has_pattern` +test @polars #string
`is_legit` +test @polars #string
`has_min` +test @polars #stats
`has_max` +test @polars #stats
`has_std` +test @polars #stats
`has_mean` +test @polars #stats
`has_sum` +test @polars #stats
`is_between` +test @polars #generic
`not_contained_in` +test @polars #generic
`not_in` +test @polars #generic
`is_contained_in` +test @polars #generic
`is_in` +test @polars #generic
`is_t_minus_n` +test @polars #datetime
`is_t_minus_1` +test @polars #datetime
`is_t_minus_2` +test @polars #datetime
`is_t_minus_3` +test @polars #datetime
`is_yesterday` +test @polars #datetime
`is_today` +test @polars #datetime
`has_percentile` +test @polars #stats
`is_inside_interquartile_range` +test @polars #stats
`has_correlation` +test @polars #ml
`satisfies` +test @polars #generic
`has_cardinality` +test @polars #ml
`has_infogain` +test @polars #ml
`has_entropy` +test @polars #ml
`is_on_weekday` +test @polars #datetime
`is_on_weekend` +test @polars #datetime
`is_on_monday` +test @polars #datetime
`is_on_tuesday` +test @polars #datetime
`is_on_wednesday` +test @polars #datetime
`is_on_thursday` +test @polars #datetime
`is_on_friday` +test @polars #datetime
`is_on_saturday` +test @polars #datetime
`is_on_sunday` +test @polars #datetime
`is_on_schedule` +test @polars #datetime
`is_daily` +test @polars #datetime
`has_workflow` +test @polars #business
`is_custom` +test @polars #generic
`is_complete` +test @duckdb #generic
`is_empty` +test @duckdb #generic
`are_complete` +test @duckdb #generic
`is_unique` +test @duckdb #generic
`is_primary_key` +test @duckdb #generic
`are_unique` +test @duckdb #generic
`is_composite_key` +test @duckdb #generic
`is_greater_than` +test @duckdb #numeric
`is_positive` +test @duckdb #numeric
`is_greater_or_equal_than` +test @duckdb #numeric
`is_in_millions` +test @duckdb #numeric
`is_in_billions` +test @duckdb #numeric
`is_less_than` +test @duckdb #numeric
`is_negative` +test @duckdb #numeric
`is_less_or_equal_than` +test @duckdb #numeric
`is_equal_than` +test @duckdb #numeric
`has_pattern` +test @duckdb #string
`is_legit` +test @duckdb #string
`has_min` +test @duckdb #stats
`has_max` +test @duckdb #stats
`has_std` +test @duckdb #stats
`has_mean` +test @duckdb #stats
`has_sum` +test @duckdb #stats
`is_between` +test @duckdb #generic
`not_contained_in` +test @duckdb #generic
`not_in` +test @duckdb #generic
`is_contained_in` +test @duckdb #generic
`is_in` +test @duckdb #generic
`is_t_minus_n` +test @duckdb #datetime
`is_t_minus_1` +test @duckdb #datetime
`is_t_minus_2` +test @duckdb #datetime
`is_t_minus_3` +test @duckdb #datetime
`is_yesterday` +test @duckdb #datetime
`is_today` +test @duckdb #datetime
`has_percentile` +test @duckdb #stats
`is_inside_interquartile_range` +test @duckdb #stats
`has_correlation` +test @duckdb #ml
`satisfies` +test @duckdb #generic
`has_cardinality` +test @duckdb #ml
`has_infogain` +test @duckdb #ml
`has_entropy` +test @duckdb #ml
`is_on_weekday` +test @duckdb #datetime
`is_on_weekend` +test @duckdb #datetime
`is_on_monday` +test @duckdb #datetime
`is_on_tuesday` +test @duckdb #datetime
`is_on_wednesday` +test @duckdb #datetime
`is_on_thursday` +test @duckdb #datetime
`is_on_friday` +test @duckdb #datetime
`is_on_saturday` +test @duckdb #datetime
`is_on_sunday` +test @duckdb #datetime
`is_on_schedule` +test @duckdb #datetime
`is_daily` +test @duckdb #datetime
`has_workflow` +test @duckdb #business
`is_custom` +test @duckdb #generic
`is_complete` +test @bigquery #generic
`is_empty` +test @bigquery #generic
`are_complete` +test @bigquery #generic
`is_unique` +test @bigquery #generic
`is_primary_key` +test @bigquery #generic
`are_unique` +test @bigquery #generic
`is_composite_key` +test @bigquery #generic
`is_greater_than` +test @bigquery #numeric
`is_positive` +test @bigquery #numeric
`is_greater_or_equal_than` +test @bigquery #numeric
`is_in_millions` +test @bigquery #numeric
`is_in_billions` +test @bigquery #numeric
`is_less_than` +test @bigquery #numeric
`is_negative` +test @bigquery #numeric
`is_less_or_equal_than` +test @bigquery #numeric
`is_equal_than` +test @bigquery #numeric
`has_pattern` +test @bigquery #string
`is_legit` +test @bigquery #string
`has_min` +test @bigquery #stats
`has_max` +test @bigquery #stats
`has_std` +test @bigquery #stats
`has_mean` +test @bigquery #stats
`has_sum` +test @bigquery #stats
`is_between` +test @bigquery #generic
`not_contained_in` +test @bigquery #generic
`not_in` +test @bigquery #generic
`is_contained_in` +test @bigquery #generic
`is_in` +test @bigquery #generic
`is_t_minus_n` +test @bigquery #datetime
`is_t_minus_1` +test @bigquery #datetime
`is_t_minus_2` +test @bigquery #datetime
`is_t_minus_3` +test @bigquery #datetime
`is_yesterday` +test @bigquery #datetime
`is_today` +test @bigquery #datetime
`has_percentile` +test @bigquery #stats
`is_inside_interquartile_range` +test @bigquery #stats
`has_correlation` +test @bigquery #ml
`satisfies` +test @bigquery #generic
`has_cardinality` +test @bigquery #ml
`has_infogain` +test @bigquery #ml
`has_entropy` +test @bigquery #ml
`is_on_weekday` +test @bigquery #datetime
`is_on_weekend` +test @bigquery #datetime
`is_on_monday` +test @bigquery #datetime
`is_on_tuesday` +test @bigquery #datetime
`is_on_wednesday` +test @bigquery #datetime
`is_on_thursday` +test @bigquery #datetime
`is_on_friday` +test @bigquery #datetime
`is_on_saturday` +test @bigquery #datetime
`is_on_sunday` +test @bigquery #datetime
`is_on_schedule` +test @bigquery #datetime
`is_daily` +test @bigquery #datetime
`has_workflow` +test @bigquery #business
`is_custom` +test @bigquery #generic
`is_complete` +test @snowpark #generic
`is_empty` +test @snowpark #generic
`are_complete` +test @snowpark #generic
`is_unique` +test @snowpark #generic
`is_primary_key` +test @snowpark #generic
`are_unique` +test @snowpark #generic
`is_composite_key` +test @snowpark #generic
`is_greater_than` +test @snowpark #numeric
`is_positive` +test @snowpark #numeric
`is_greater_or_equal_than` +test @snowpark #numeric
`is_in_millions` +test @snowpark #numeric
`is_in_billions` +test @snowpark #numeric
`is_less_than` +test @snowpark #numeric
`is_negative` +test @snowpark #numeric
`is_less_or_equal_than` +test @snowpark #numeric
`is_equal_than` +test @snowpark #numeric
`has_pattern` +test @snowpark #string
`is_legit` +test @snowpark #string
`has_min` +test @snowpark #stats
`has_max` +test @snowpark #stats
`has_std` +test @snowpark #stats
`has_mean` +test @snowpark #stats
`has_sum` +test @snowpark #stats
`is_between` +test @snowpark #generic
`not_contained_in` +test @snowpark #generic
`not_in` +test @snowpark #generic
`is_contained_in` +test @snowpark #generic
`is_in` +test @snowpark #generic
`is_t_minus_n` +test @snowpark #datetime
`is_t_minus_1` +test @snowpark #datetime
`is_t_minus_2` +test @snowpark #datetime
`is_t_minus_3` +test @snowpark #datetime
`is_yesterday` +test @snowpark #datetime
`is_today` +test @snowpark #datetime
`has_percentile` +test @snowpark #stats
`is_inside_interquartile_range` +test @snowpark #stats
`has_correlation` +test @snowpark #ml
`satisfies` +test @snowpark #generic
`has_cardinality` +test @snowpark #ml
`has_infogain` +test @snowpark #ml
`has_entropy` +test @snowpark #ml
`is_on_weekday` +test @snowpark #datetime
`is_on_weekend` +test @snowpark #datetime
`is_on_monday` +test @snowpark #datetime
`is_on_tuesday` +test @snowpark #datetime
`is_on_wednesday` +test @snowpark #datetime
`is_on_thursday` +test @snowpark #datetime
`is_on_friday` +test @snowpark #datetime
`is_on_saturday` +test @snowpark #datetime
`is_on_sunday` +test @snowpark #datetime
`is_on_schedule` +test @snowpark #datetime
`is_daily` +test @snowpark #datetime
`has_workflow` +test @snowpark #business
`is_custom` +test @snowpark #generic
`not_between` +family #generic {cm:2025-02-09}
`not_between` +engine @bigquery #generic
`not_between` +engine @duckdb #generic
`not_between` +engine @pandas #generic
`not_between` +engine @polars #generic
`not_between` +engine @pyspark #generic
`not_between` +engine @snowpark #generic
`not_between` +test @bigquery #generic
`not_between` +test @duckdb #generic
`not_between` +test @pandas #generic
`not_between` +test @polars #generic
`not_between` +test @pyspark #generic
`not_between` +test @snowpark #generic
